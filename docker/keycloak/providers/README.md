# Keycloak Admin Event HTTP Listener

Place the HTTP admin-event listener JAR compatible with Keycloak 24.x in this folder.
Example: `keycloak-http-event-listener-24.0.4.jar`.

The container mounts this directory to `/opt/keycloak/providers` via `docker-compose.yml`.
